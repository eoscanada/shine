package main

import (
	"testing"
)

func TestRunContract(t *testing.T) {
	// require.NoError(t, handleCommand("user1", "post.1", "/recognize user2 for shit show")) // r1
	// require.NoError(t, handleCommand("user2", "post.2", "/recognize user3 for shit show")) // r2
	// require.NoError(t, handleCommand("user1", "post.3", "/upvote post.1"))
	// require.NoError(t, handleCommand("user3", "post.4", "/upvote post.2"))
	// require.NoError(t, handleCommand("user1", "post.5", "/register eos.user1"))
	// require.NoError(t, handleCommand("user2", "post.6", "/register eos.user2"))
	// require.NoError(t, sendMoneyToContract("1000.0000 EOS"))
}

// func handleCommand(fromUser, command string) error {
// 	// handler for /recognize
// 	// handler for
// }

// func sendMoneyToContract(amount string) error {
// }
